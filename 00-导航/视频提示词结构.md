---
# auto-generated by build_vault.py —— 本文件由脚本生成，重跑会覆盖
type: 方法
---

# 视频提示词结构

> [!abstract] 文生视频 ≠ 文生图 + 「会动」
> 视频模型需要你额外说清楚三件事：**谁在动、怎么动、镜头怎么动**。
> 图像提示词只覆盖了「画面长什么样」，剩下的全靠这一页。

## 一、主公式

```
主体 + 场景 + 运动 + 美学控制 + 风格化
```

- **基础**：主体 + 场景 + 运动
- **进阶**：以上 + 光照 / 时段 / 景别 / 构图 / 镜头 / 色调
- **图生视频**：运动描述 + 运镜（因为主体和场景已经由首帧给定了）

## 二、必须显式声明的五个维度

视频模型不会自己推断这些，你不写它就随机。

### 1. 运动 Motion
`walks slowly` / `turns her head` / `hair drifts in the wind` / `rain falls steadily` / `camera tracks alongside`

### 2. 景别 Shot Size
`extreme close-up (ECU)` · `close-up (CU)` · `medium close-up (MCU)` · `medium shot (MS)`
· `medium wide shot (MWS)` · `wide shot (WS)` · `extreme wide shot (EWS)` · `establishing shot`

### 3. 机位 Camera Angle
`eye-level` · `high angle` · `low angle` · `dutch angle` · `aerial` · `bird's eye` · `over-the-shoulder`

### 4. 运镜 Camera Movement
`static shot` · `camera pushes in / dolly in` · `camera pulls back / dolly out`
· `pan left / right` · `tilt up / down` · `tracking shot` · `arc shot` · `crane shot`
· `handheld` · `steadicam` · `whip pan` · `compound move`

### 5. 时段与光照 Time & Light
时段：`sunrise` · `dawn` · `daylight` · `dusk` · `sunset` · `night`
光源：`sunny` · `overcast` · `moonlight` · `firelight` · `practical lighting` · `neon`
类型：`soft` · `hard` · `rim` · `backlight` · `silhouette` · `low contrast` · `high contrast`

## 三、把流派卡接到视频上

每个流派卡的「五、AI 视频层」已经给好了该流派的 motion 和 camera。

```
[流派卡的 style + lighting + color + medium]
+ [流派卡的 motion] + [流派卡的 camera]
+ [你要的主体在做什么]
```

> [!important] 一个流派一种运镜
> 运镜本身带风格信息。给印象派用手持快切、给表现主义用平稳滑轨，
> 都是错的——**运镜和流派必须同源**，每张卡里都标了。

## 四、常见错误

| 错误 | 后果 | 修正 |
|---|---|---|
| 只写画面不写运动 | 模型给一个几乎静止的镜头 | 显式写 motion |
| 写 `camera moves` | 运镜方向随机 | 写 `dolly in` / `pan left` 等具体术语 |
| 运动描述过多 | 画面崩坏、闪烁 | 一个镜头只做 1–2 个动作 |
| 忘了写景别 | 一会儿全身一会儿特写 | 每次生成都明确 shot size |
| 光照和流派冲突 | 风格被冲淡 | 视频的光照层直接复用流派卡里的 |

## 五、来源

本页结构参照开源社区的 Wan 2.2 提示词指南整理：
[EricRollei/Local_LLM_Prompt_Enhancer — WAN_GUIDE_REFERENCE.md](https://github.com/EricRollei/Local_LLM_Prompt_Enhancer/blob/main/docs/WAN_GUIDE_REFERENCE.md)

同类模型（Veo / Kling / Runway / HunyuanVideo）的结构基本一致，
差异主要在运镜术语的接受度上。
